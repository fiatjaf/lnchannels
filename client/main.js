/** @format */

window.H = window.Highcharts

window.aliasPromises = {}

const main = document.querySelector('main')
import App from './App.html'

const app = new App({
  target: main
})

export default app
