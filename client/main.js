/** @format */

window.H = window.Highcharts

window.namePromises = {}

const main = document.querySelector('main')
import App from './App.html'

const app = new App({
  target: main
})

export default app
